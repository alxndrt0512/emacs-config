;;;; whichkey-init --- whichkey config

;;; whichkey-init.el
;;; Commentary:

;; This file contains config of whichkey

;;; Code:

(use-package which-key
  :ensure t
  :config
  (which-key-mode)
  )

(provide 'whichkey-init)
;;; whichkey-init.el ends here
