;;;; replace-key --- replace key

;;; replace-key.el
;;; Commentary:

;; This file contains config of replace key

;;; Code:

(global-set-key (kbd "C-r") 'replace-regexp)

(provide 'replace-key)
;;; replace-key.el ends here
